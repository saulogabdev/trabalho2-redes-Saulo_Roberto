server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Redireciona chamadas da API internamente na rede do Docker
    location /api/ {
        proxy_pass http://backend:3000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}